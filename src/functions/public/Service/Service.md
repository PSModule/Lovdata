# Service

Operational checks that answer two questions before a script starts pulling data: is the Lovdata API
reachable, and which build is deployed. Both endpoints are open, so these commands work with no account
and no `API` key, which makes them usable as a first-run diagnostic.
